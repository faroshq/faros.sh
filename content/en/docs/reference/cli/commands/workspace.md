---
title: "railgrid workspace"
linkTitle: "workspace"
description: "Workspaces of an organization, and who is in them"
weight: 70
doc_type: Reference
generated: true
---
<!-- Generated by scripts/sync-cli-docs.py from railgrid/railgrid docs/cli. Do not edit by hand; run `make sync-cli-docs`. -->

## Synopsis

A workspace is the Kubernetes-style API you work in: edges, providers and
their resources live there, and access is per workspace.

```bash
railgrid workspace list                      # workspaces in the current org
railgrid workspace list --org acme
railgrid workspace members                   # members of the current workspace
railgrid workspace members --workspace platform
railgrid workspace create "Platform"
railgrid use --workspace platform            # make it the kubectl target
```

```bash
railgrid workspace [flags]
```

## Options

```text
  -h, --help               help for workspace
      --org string         Organization display name or UUID (default: the org that owns the kubeconfig's workspace)
      --workspace string   Workspace display name or UUID (default: the workspace the kubeconfig points at)
```

## Inherited options

```text
      --insecure-skip-tls-verify   Skip TLS certificate verification when talking to the hub
      --kubeconfig string          Path to the kubeconfig file (default: $KUBECONFIG, then ~/.kube/config)
```

## See also

- [railgrid](/docs/reference/cli/commands/) — railgrid: an open-source control plane for platform teams
- [railgrid workspace create](/docs/reference/cli/commands/workspace-create/) — Create a workspace in an organization
- [railgrid workspace list](/docs/reference/cli/commands/workspace-list/) — List the workspaces of an organization
- [railgrid workspace members](/docs/reference/cli/commands/workspace-members/) — List and change who has access to the workspace
