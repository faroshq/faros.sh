---
title: "railgrid skills"
linkTitle: "skills"
description: "Install agent skills from the railgrid repository into Claude Code and Codex"
weight: 62
doc_type: Reference
generated: true
---
<!-- Generated by scripts/sync-cli-docs.py from railgrid/railgrid docs/cli. Do not edit by hand; run `make sync-cli-docs`. -->

## Synopsis

Fetch the skills/ directory of github.com/railgrid/railgrid (or another
repository with the same layout) and install each skill where Claude Code
and Codex discover them:

```bash
Claude Code   ~/.claude/skills/<name>   (--scope user, the default)
              .claude/skills/<name>    (--scope project)
Codex         ~/.agents/skills/<name>
              .agents/skills/<name>
```

The skills are read from GitHub at install time, so you always get the
current main branch (or the --ref you name) regardless of the CLI version.
Re-run install to update. Directories that railgrid installed are replaced;
directories you wrote yourself are left alone unless you pass --force.

Skills load when an agent session starts, so restart Claude Code or Codex
after installing.

```bash
railgrid skills [flags]
```

## Options

```text
  -h, --help   help for skills
```

## Inherited options

```text
      --insecure-skip-tls-verify   Skip TLS certificate verification when talking to the hub
      --kubeconfig string          Path to the kubeconfig file (default: $KUBECONFIG, then ~/.kube/config)
```

## See also

- [railgrid](/docs/reference/cli/commands/) — railgrid: an open-source control plane for platform teams
- [railgrid skills install](/docs/reference/cli/commands/skills-install/) — Install skills for Claude Code and Codex (all skills by default)
- [railgrid skills list](/docs/reference/cli/commands/skills-list/) — List the skills available in the repository
