---
title: "railgrid edge"
linkTitle: "edge"
description: "Create, list, inspect and remove edges (clusters and servers)"
weight: 29
doc_type: Reference
generated: true
---
<!-- Generated by scripts/sync-cli-docs.py from railgrid/railgrid docs/cli. Do not edit by hand; run `make sync-cli-docs`. -->

## Synopsis

An edge is a Kubernetes cluster or a Linux server that runs the railgrid agent
and dials out to the hub. Once connected, 'railgrid connect' points kubectl at a
cluster edge and 'railgrid ssh' opens a shell on a server edge.

```bash
railgrid edge create my-cluster                  # prints the join command
railgrid edge create my-vps --type server
railgrid edge list
railgrid edge get my-cluster -o yaml
railgrid edge kubeconfig my-cluster -o ./my-cluster.kubeconfig
railgrid edge delete my-vps
```

```bash
railgrid edge [flags]
```

## Options

```text
  -h, --help   help for edge
```

## Inherited options

```text
      --insecure-skip-tls-verify   Skip TLS certificate verification when talking to the hub
      --kubeconfig string          Path to the kubeconfig file (default: $KUBECONFIG, then ~/.kube/config)
```

## See also

- [railgrid](/docs/reference/cli/commands/) — railgrid: an open-source control plane for platform teams
- [railgrid edge create](/docs/reference/cli/commands/edge-create/) — Create an edge and print its join command
- [railgrid edge delete](/docs/reference/cli/commands/edge-delete/) — Delete an edge (the agent on it loses hub access)
- [railgrid edge get](/docs/reference/cli/commands/edge-get/) — Show an edge's connection status and details
- [railgrid edge join-command](/docs/reference/cli/commands/edge-join-command/) — Print the agent join command for an edge
- [railgrid edge kubeconfig](/docs/reference/cli/commands/edge-kubeconfig/) — Print or merge a kubeconfig for a Kubernetes edge
- [railgrid edge list](/docs/reference/cli/commands/edge-list/) — List edges
- [railgrid edge upgrade](/docs/reference/cli/commands/edge-upgrade/) — Print upgrade instructions for an edge agent
